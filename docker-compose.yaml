version: "2.1"
networks: {web: {name: web}}

services:
  web:
    image: traefik
    ports:
      - 443:443
      - 80:80
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik.toml:/traefik.toml:ro
      - ./acme:/acme:rw
    networks: [web]
